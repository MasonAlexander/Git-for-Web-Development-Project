 
    https://codepen.io/Masond570/pen/oNYmvOL
 
    1. What is Semantic HTML? Semantic HTML defines the content on a web page rather than the visuals
    2. What is HTML used for? HTML structures the web page and the content within it.
    3. What is an attribute and where do we put it? Attributes define characteristics of an element like width or height in an img tag.
    4. What is the h1 tag used for? How many times should I use it on a page? The heading or title. Once.
    5. Name two tags that have required attributes the <img> tag and the <a> tag
    6. What do we put in the head of our HTML document?  the page title and links to CSS
    7. What is an id? it points to a style specified by css or javascript 
    8. What elements can I add an id to? any element
    9. How many times can I use the same id on a page? Once
    10. What is a class? a class is similar to an ID but can be applied to multiple elements 
    11. What elements can I add a class to? any html element
    12. How many times can I use the same class on a page? once 
    13. How do I get my link to open in a new tab? add target="_blank" to an anchor tag
    14. What is the alt attribute in the image tag used for? it adds a text description if the user cant view the image
    15. How do I reference an id? #
    16. What is the difference between a section and a div? a section should be related content grouped together, a div is a container 
    17. What is CSS used for? styling a webpage
    18. How to we select an element? Example - every h2 on the page h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one i might want to style every h2 on my page to be red,
        instead of creating ID's for every h2 on the page. I may want to use an ID on something like a menu or a header since they only appear once.
    20. How do we select classes in CSS? the .class selector
    21. How do we select a p element with a single class of “human””? p.human
    22. What is a parent child selector? When would this be useful? it selects all elements that are a direct child of the element and I'm not sure.
    23. How do you select all links within a div with the class of sidebar? a.sidebar
    24. What is a pseudo selector? it defines a special state like like :hover
    25. What do we use the change the spacing between lines? line-height
    26. What do we use to change the spacing between letters? letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform:
    28. How do I add a 1px border around my div that is dotted and black?   div {border: 1px dotted #000;}
    29. How do I select everything on the page? *{}
    30. How do I write a comment in CSS? /* this is a comment */
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm because you can't get it back and you won't be prompted before it deletes
    34. Why should I use version control? It's easy to keep track of changes, rollback an old version, and collaborate with other people on your project.
    35. How often should I commit to github? Often
    36. What is the command we would use to push our repo up to github? git push origin -u firstName-lastName
    37. Walk me through Lambda's git flow.  
     FORK the repo on Github. 
     then CLONE (git clone url) also make sure to cd  to the repo file 
     create your branch (git checkout -b 'firstName-lastName')
     Make your changes and add your files
     then COMMIT (git add .) make sure its ready to commit with (git status) then (git commit -m 'specific message goes here') confirm it suceeded with (git status)
     then PUSH (git push origin -u firstName-lastName)
     then PULL (make sure to select your branch and not lambda school's) then compare to your branch
